# V9.1an 匯出選單圖標語意共用

> 狀態：closed
>
> 工作單位：書籍及地圖的匯出選單共用既有相同字形的語意圖標。

## 範圍與非目標

- 書籍總覽匯出選單與地圖 PDF 匯出選單皆使用 `square.and.arrow.up`，語意同為匯出。
- 新增 `SailuneSymbol.export` 並替換這兩個直接呼叫；字形、文字、tooltip、選單項目與實際匯出流程不變。
- 首頁發布入口雖同字形但不是匯出，保留原用途；TXT／EPUB 子項目沿用各自的專用圖標。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能統一圖標與程式碼。 |
| U | not_applicable | 不改字形、文案及版位。 |
| I | approved | 兩處匯出選單標籤只改圖標來源。 |

## 完成條件

- 兩個匯出選單共用 `.export`；發布與子項目保留獨立語意。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；不新增或執行 XCTest。

## 結果與驗證

- 兩個匯出選單已引用 `.export`；發布與 TXT／EPUB 子項目保留各自用途。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；`SailuneSymbol` 靜態呼叫點 180，直接 `systemName`／`systemImage` 字串 102。未執行 XCTest 或 GUI 驗收。
