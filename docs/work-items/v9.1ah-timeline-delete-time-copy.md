# V9.1ah 時間軸刪除時間描述統一

> 狀態：closed
>
> 工作單位：讓三個相同的時間格刪除控制共用既有操作描述資源。

## 範圍與非目標

- `TimelineViews.swift` 三個「刪除這個時間」純圖標按鈕已使用 `SailuneSymbol.deleteTime`，但各自重複寫 accessibility label 與 help 字面值。
- 本批將這六個描述參數改讀既有 `SailuneActionCopy.deleteTime`；String Catalog 的繁體中文值仍為「刪除這個時間」。
- 不改三個按鈕的 frame、padding、font、位置、命中範圍、style、刪除 action、確認或資料操作。24 pt 按鈕命中是否符合 V9 基準留給實際畫面驗收。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同操作共用圖標與敘述文字。 |
| U | not_applicable | 文字值、圖標與畫面不變。 |
| I | approved | 六個字面值參數改用既有語意文案 key，不改按鈕外殼。 |

## 完成條件

- 三個按鈕各自的 help／accessibility label 均讀同一描述；原繁體中文值不變。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 三個時間格刪除按鈕的六個 help／accessibility label 參數均已改讀 `SailuneActionCopy.deleteTime`；原圖標、尺寸、位置、action 與中文值不變。
- `git diff --check`、String Catalog 原值核對及主機無簽章 macOS Debug build 通過。
- 未執行 XCTest 或該按鈕實際命中／VoiceOver 驗收；當時 Mac 鎖定，後續已解鎖但本批未操作會刪除資料的控制。
