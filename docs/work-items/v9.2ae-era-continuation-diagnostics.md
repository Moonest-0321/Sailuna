# V9.2ae 改元接續提示查詢診斷

> 狀態：closed
>
> 工作單位：區分改元提示的正常無紀元與查詢錯誤，保留現有畫面結果。

## 現況與範圍

- `EraChangePopover.continuationText` 先查本書最後紀元，再查該紀元最後年份；兩次 `try?` 在失敗時分別回傳沒有接續提示或預設第 1 年，與正常空資料同形。
- 本批只將兩次查詢改為 `do/catch`，以 private OSLog 記錄查詢名稱、書籍／紀元 ID 與錯誤。正常空資料、失敗時的可見提示結果、改元 action、資料和 schema 不變。
- 不處理查詢失敗與真正空資料在畫面上的區分；這屬後續 UI 政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者授權 V9 內部失敗路徑整理；依已核定「先只記錄錯誤，不改操作結果」的保守範圍處理。 |
| U | not_applicable | 不更改可見文字或互動。 |
| I | approved | 用具名 `do/catch` 取代兩處 `try?` 並記 private OSLog，維持原 optional／第 1 年 fallback。 |

## 完成條件

- 無紀元與查詢失敗仍回傳 `nil` 提示；最後年份無值或查詢失敗仍顯示第 1 年，且只在錯誤時記日誌。
- 改元流程與呼叫點不變；`git diff --check` 及主機無簽章 macOS Debug build 通過。
- 不新增或執行 XCTest；故障注入與 GUI 驗收另記未驗證。

## 結果與驗證

- `EraChangePopover.continuationText` 的最後紀元與最後年份讀取都已在錯誤時記 private OSLog；正常空資料及錯誤時的原畫面結果不變。
- 專案 `try?` 靜態命中由 15 處降至 13 處；`git diff --check` 與主機無簽章 macOS Debug build 通過。
- 未執行 XCTest、故障注入或 GUI 驗收；診斷可觀測性與資料錯誤 UI 政策尚未實測／核定。
