# V9.2u 時間軸事件卡片投影責任歸位

> 狀態：closed（純責任搬移完成）

## 工作單元

- 目標：將 `TimelineCardPresentation` 與 `TimelineCardProjection` 從大型 `TimelineViews.swift` 搬到 `TimelineCardProjection.swift`，讓事件卡片的來源定位、節錄和無障礙文字有單一專責位置。
- 完成條件：兩段宣告正文、存取層級及 `@MainActor` 維持；`TimelinePanelView` 和 `TimelineEventCardView` 呼叫點不改；新檔納入主機 Debug build，`git diff --check` 通過。
- 範圍：上述兩段純投影宣告與必要 `Foundation` 匯入。
- 非目標：不改事件卡片 View、文案、節錄演算法、Store API、資料保存、schema 或畫面互動。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求完成 V9 內部程式責任整理；本切片僅搬移已獨立命名的投影。 |
| U | not_applicable | 不變更畫面或互動。 |
| I | approved | 使用者已明確指示完成 V9；函式及型別逐字搬移，呼叫端不變。 |

## 驗證與限制

- 2026-09-24：兩段宣告分別為 6 行與 75 行，與 `HEAD:TimelineViews.swift` 原正文逐字相同；原檔無重複宣告，卡片 View 與面板的呼叫點未改。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；本批未新增或執行程式測試。
- 建置只證明可編譯，不能單獨證明事件卡片在實際資料下的視覺及內容結果。
