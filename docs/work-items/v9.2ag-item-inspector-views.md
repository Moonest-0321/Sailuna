# V9.2ag 物品 Inspector 畫面責任歸位

> 狀態：closed
>
> 工作單位：將物品清單、詳情、副本及等級編輯 View 從 Inspector 導覽檔移到專責檔。

## 現況與範圍

- `InspectorViews.swift` 中的六個連續物品 View 構成完整物品功能區；`InspectorRootView` 呼叫物品清單、詳情及副本詳情三個入口。
- 本批搬移 `ItemListContainerView`、`ItemReferenceRow`、`ItemDetailView`、`ItemCopyDetailView`、`ItemLevelEditor`、`ItemLevelRow` 至 `ItemInspectorViews.swift`。只有三個跨檔入口由 file-private 改為 module internal，其他宣告及 View 本文保留。
- 不改物品／副本／等級資料操作、正文引用、歷史定位、刪除、UI 文字／圖標／位置、schema 或保存政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者授權 V9 內部責任整理；物品功能區有明確獨立於 Inspector 導覽的畫面邊界。 |
| U | not_applicable | 不改 UI 呈現或互動。 |
| I | approved | 從目前工作樹原樣搬移六個 View，僅調整跨檔入口的存取層級及新檔 import。 |

## 完成條件

- 六個 View 各宣告一次，原 `InspectorRootView` 呼叫仍編譯。
- 搬移段落除三處存取層級外與搬移前目前工作樹一致；私有輔助 View 仍限制在新檔。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 六個 View 共 647 行已移至 `ItemInspectorViews.swift`；把三個跨檔入口的 `private` 還原後，搬移段落 SHA-256 與搬移前工作樹同為 `792ddf4ae401b19acd3dd8f900a7c485a9feb2738b5dd21b2f62f59139b024d2`。
- 六個 View 各僅在新檔宣告一次；`InspectorRootView` 保留原三個呼叫。`InspectorViews.swift` 目前為 1682 行，物品群組由 651 行專責檔承接（含 import）。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；未執行 XCTest 或物品畫面 GUI 驗收。
