# V9.1h xmark 操作語意接入

> 狀態：closed（12 個畫面呼叫點已依操作語意接入）
>
> 工作單位：將相同 `xmark` 字形依操作目的接到具名圖標 key。

## 目標與完成條件

- 2026-09-23 初篩：12 個畫面呼叫點使用 raw `"xmark"`；語意表另有兩處字面對照。
- 依實際 action 分為 `.close`（關閉畫面／提示）、`.cancel`（取消改名／審核）、`.removeSelection`（移除 AI 模板或閱讀範圍）、`.removeRelatedCharacter`（解除事件的角色關聯）與 `.deleteTime`（時間軸刪除日期）。
- 全部保留原 `xmark` 字形、按鈕內容、文字、位置、role、action、命中區與資料操作。
- 靜態確認沒有 raw 畫面字面值，並通過差異檢查及 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標一致，也要求不同功能不要因圖形相同而混淆；Timeline 刪除時間的既有字形維持已批准的呈現。 |
| U | not_applicable | 圖形、排版、tooltip、accessibility 與互動保持原樣。 |
| I | approved | 使用者已指示完成 V9；本批限定 12 個已逐項核對 action 的圖標來源。 |

## 驗證與下一步

- 2026-09-23：12 個畫面呼叫點已分類：關閉 3、取消 3、AI 選取移除 2、事件角色解除關聯 1、時間軸刪除時間 3。原 raw `"xmark"` 只留在 `SailuneSymbol` 的字形對照。
- 2026-09-23：原圖形、action 與修飾未改；`git diff --check` 和主機環境無簽章 Debug build 通過。未執行程式測試或 GUI 驗收。
