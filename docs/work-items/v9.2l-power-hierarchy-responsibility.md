# V9.2l 勢力層級與直屬關係責任歸位

> 狀態：closed（三個宣告已原樣歸位）
>
> 工作單位：把 `PowerHierarchyError`、`PowerHierarchyStore` 與 `PowerGraphStore` 從同時保存歷史 schema 和一般設定 Store 的 `V5SettingModels.swift` 移入勢力階層專責檔。

## 目標與完成條件

- 三個宣告正文逐字搬移；型別名稱、actor 標記、存取層級、API、錯誤文案、驗證與保存順序維持原狀。
- `V5SettingsStore` 與功能 View 的呼叫點不改，歷史 V1–V13 schema／migration snapshot 不動。
- 新檔只匯入原宣告所需的 `Foundation` 與 `SwiftData`。
- 核對原檔沒有重複宣告，建置成功；不以單純行數減少宣稱耦合已解決。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 對內部責任做規範，讓新功能與維護更容易；此處的階層不變條件與一般設定模型有明確責任差異。 |
| U | not_applicable | 純宣告歸位，無 UI、文字或互動變更。 |
| I | approved | 使用者已指示完成 V9；搬移限於上述三個宣告，歷史 schema 不改。 |

## 驗證與限制

- 2026-09-24：新檔的 `PowerHierarchyError`、`PowerHierarchyStore`、`PowerGraphStore` 三段正文與 `HEAD:V5SettingModels.swift` 對應區塊逐字相同，原檔已無重複宣告；Store／View 呼叫點未修改。
- `git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
- 此工作不修改階層演算法、跨 Store 刪除、補償或錯誤回報政策。
