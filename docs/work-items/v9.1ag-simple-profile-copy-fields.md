# V9.1ag 筆名與副本欄位共用接線

> 狀態：closed
>
> 工作單位：將四個相同原生圓角樣式的單行文字欄接入既有共用元件。

## 範圍與完成條件

- `AuthorSettingsView` 和書櫃個人資料表單各一個筆名欄，物品副本名稱欄及角色勢力職稱欄，共四處使用 `SailuneFormTextField`。
- 保留原 title、Binding、`.roundedBorder`、作者設定字體、物品副本 `onChange` 保存、列內位置與周圍 layout。
- 不處理勢力識別區的容器層級樣式、世界條目名稱焦點、改名／搜尋／新書提交、數值或多行欄；不改 schema、資料與保存政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求同種欄位統一呼叫共用程式；四處皆符合 V9.1m 既有元件契約。 |
| U | not_applicable | 原 TextField、原生圓角樣式與版面不變。 |
| I | approved | 只以既有元件取代四組 `TextField`＋`.roundedBorder`，其餘 modifier 留在原處。 |

## 驗證

- 四處來源碼接線、`git diff --check` 和主機無簽章 macOS Debug build；不新增或執行 XCTest。
- CJK、首行內距、focus 與 VoiceOver 仍屬 V9.1x／整體 GUI 驗收，不以編譯代替畫面驗收。

## 結果

- 四處已接入 `SailuneFormTextField`；原 Binding、title、作者設定字體與物品副本 `onChange` 均保留。
- 共用一般單行欄位引用由 17 增至 21 處；原生 `TextField(` 命中由 87 降至 83 處，`.roundedBorder` modifier 由 50 降至 46 處，其中一處仍在共用元件內。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；未執行 XCTest 或 GUI 驗收。
