# V9.2ac 地點與世界條目畫面責任歸位

> 狀態：closed
>
> 工作單位：將地點及世界條目的清單／詳情 View 從勢力與側欄設定共用檔案移入專責檔案。

## 現況與範圍

- `V5SettingViews.swift` 前段包含側欄設定、勢力清單／層級／詳情及勢力關係編輯；尾段自 `PlaceListView` 起包含地點及世界條目的兩個清單和兩個詳情 View。
- 本批原樣搬移尾段四個 View 到 `PlaceWorldTermViews.swift`，保持 module 可見名稱與原呼叫點。設定搜尋、預設資料、Store action、刪除與保存流程仍各由原責任者提供。
- 不改欄位、文字、圖標、位置、焦點、SwiftData Binding、錯誤提示、保存／刪除、schema 或資料。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權 V9 內部程式責任整理；這四個畫面構成獨立的地點／世界條目功能區。 |
| U | not_applicable | 只改宣告位置，不改 UI 呈現。 |
| I | approved | 從目前工作樹逐字搬移四個 View，僅新檔增加必要 import。 |

## 完成條件

- 四個 View 各只在新檔宣告一次，`InspectorViews.swift` 與彼此的呼叫可編譯。
- 搬移段落正文與搬移前工作樹一致；`V5SettingViews.swift` 保留勢力及側欄設定責任。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 從目前工作樹逐字搬移尾段 467 行至新檔；搬移段落 SHA-256 為 `141bca3c7415eaf54a0bf5716e4a730d425d92aecc971cf269749363db4a48e4`，新檔對應段落核對一致。
- 四個 View 均僅在新檔宣告一次；原檔保留側欄與勢力畫面。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。未執行 XCTest 或這四個 View 的 GUI 驗收。
- 後續 V9.2ad 已把當時留在 `V5SettingViews.swift` 的側邊欄與勢力 View 再移到各自具名檔；本批地點／世界條目檔案未受影響。
