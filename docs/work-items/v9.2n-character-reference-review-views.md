# V9.2n 角色參照審核與組字欄位歸位

> 狀態：closed（兩個 View 宣告已歸位）
>
> 工作單位：將 `UnlinkedReferenceReviewView` 與 `CompositionAwareTextField` 從總覽型 `InspectorViews.swift` 移到各自責任檔。

## 完成條件

- 審核 View 的候選勾選、全選／全不選、套用／暫不處理與原有文案、樣式維持原樣。
- AppKit 組字輸入欄的 `NSTextView` 設定、delegate、Binding 更新、開始／結束編輯 callback 原樣保留。
- 原 `CharacterDetailView` 呼叫點、狀態及同步流程不變；跨檔使用的型別由檔案私有改為模組內可見。
- 不改正文連結、SwiftData schema、畫面排版或輸入行為。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 整理內部責任；審核呈現與專用輸入封裝是兩個獨立於 Inspector 導覽的責任。 |
| U | not_applicable | 只搬移既有 View 本文，無可見內容或互動變更。 |
| I | approved | 使用者已指示完成 V9；改動限於兩個宣告和必要存取層級。 |

## 驗證與限制

- 2026-09-24：`CompositionAwareTextField` 本文與 `HEAD:InspectorViews.swift` 對應區塊相同；`UnlinkedReferenceReviewView` 除先前 V9 已把 `xmark` 換成同字形的 `SailuneSymbol.cancel` 外亦相同。兩個型別只把檔案私有改為模組內可見，原呼叫點未改。
- `git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
- 組字、焦點、VoiceOver、CJK 輸入仍需實際 App 驗收，不能由編譯結果推論。
