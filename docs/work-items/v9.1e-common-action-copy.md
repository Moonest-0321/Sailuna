# V9.1e 通用操作文字接入

> 狀態：closed（58 個通用操作文字呼叫點已接入）
>
> 工作單位：讓語意明確的取消／儲存按鈕重用同一文字資源。

## 目標與完成條件

- 將現有 48 個 `Button("取消"...)` 和 10 個 `Button("儲存"...)` 呼叫點改用 `SailuneActionCopy.cancel`／`.save`，保持原有中文顯示值。
- 兩個原本附帶儲存圖標的按鈕透過 `SailuneSymbol.save` 取得原 `square.and.arrow.down` 圖形；相同圖形用於匯入地圖時保留原呼叫，因操作語意不同。
- 不更改 Button role、action、鍵盤捷徑、排列、tooltip、確認流程或其他文字。
- 靜態核對呼叫點、String Catalog 值與差異，並確認 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者明確要求同功能使用相同敘述文字並集中程式碼呼叫。 |
| U | not_applicable | 中文顯示值與原有圖形不變；本批只更換資源來源。 |
| I | approved | 使用者已指示完成 V9；本批限定兩個確定的通用按鈕文字與兩個既有儲存圖標。 |

## 驗證與下一步

- 2026-09-23：48 個 `Button("取消"...)` 和 10 個 `Button("儲存"...)` 已改用共用文字資源；兩個原帶儲存圖標的按鈕改用 `SailuneSymbol.save`，保留原圖形。原兩種 Button 字面呼叫點已清空。
- 2026-09-23：建置產物 `Localizable.strings` 中 `action.cancel`／`action.save` 分別仍為「取消」／「儲存」；`git diff --check` 和主機環境無簽章 Debug build 通過。未執行程式測試或 GUI 驗收。
