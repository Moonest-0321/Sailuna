# 帆夢（Sailune）協作規則

## 對話上下文與開始工作

- 每個新對話第一次工作時，依序讀取本文件、`docs/project-status.md`、`docs/work-items/current.md` 與 `docs/handoffs/current.md`；若兩份 current 文件皆為 `closed`，只需確認沒有未完成工作。
- 接手交接內容後，先以 `git status`、`git diff`、相關程式與測試驗證交接敘述；交接檔是導航，不是最終事實來源。
- 只讀取與當前任務直接相關的 `docs/spec-*.md`、技術文件或測試報告。
- 若文件在本對話中沒有修改，後續工作沿用已建立的上下文；只有任務範圍改變、文件被更新或上下文不確定時才重新讀取。
- 修改程式前遵守 `docs/coding-standards.md` 的相關章節；不需要載入整份規範。
- 技術架構或資料操作則讀取對應的 `architecture.md`、`data-model.md` 或 `backup-and-migration.md`。
- 以目前程式、測試與文件共同判斷現況；不要只依提交訊息推測功能。
- 注意工作目錄中可能已有使用者未提交的修改，不要覆蓋或回復它們。
- 完整開發、檢查點與聊天交接流程見 `docs/development-workflow.md`。

## 工作單元與檢查點

- 每次工作先定義一個可驗證的工作單元：目標、完成條件、相關範圍與明確非目標。
- 新功能或流程改動先建立 `docs/work-items/current.md`，依序經過需求（R）、UI（U）與實作計畫（I）三個批准點；切換聊天不得改變批准狀態。
- 使用者說明需求後，先整理現況、問題、範圍、非目標、限制、驗收條件與待確認事項；R 未批准前不得開始功能實作。
- 涉及畫面或互動時，先提供使用流程、資訊架構及足以評估的文字線框、圖片 mockup 或互動原型；U 未批准前不得把提案直接實作成正式功能。沒有 UI 影響時，在工作單記為 `not_applicable` 並說明原因。
- R 與 U 完成後，提出受影響範圍、資料相容性、工作拆解、風險及測試計畫；只有使用者明確表示開始實作，才能把 I 記為 `approved` 並修改功能程式。
- 小型明確修正、純文件工作、唯讀調查，或使用者已直接授權的具體變更，可以略過不適用關卡，但需在工作單或交接檔記錄理由。
- 在產品決策、schema／遷移、跨模組行為、測試結果或阻礙發生變化後，更新 `docs/handoffs/current.md`。
- 對話變長、準備切換聊天或即將暫停時，必須先建立檢查點；不要等上下文快滿時才回想整段對話。
- 檢查點需區分「已決定」、「暫時假設」、「待確認」，並記錄決策理由及被否決方案；不可只留下結論。
- 工作單保存需求、UI、驗收條件、批准狀態與實作／測試計畫；交接檔保存工作樹、進度、測試、阻礙與唯一下一步，兩者不可互相取代。
- 交接需列出使用者原有修改與本工作修改的邊界、測試命令／結果、第一個下一步，以及新聊天應讀取的最小文件集合。

## 修改原則

- 不確定某個既有功能是否仍需要時，先保留並標記為待確認。
- 首頁的任何彈窗、浮動面板或自訂 modal 都必須允許點擊空白處取消；空白遮罩需攔截點擊，不得穿透到背景操作。
- 首頁的短輸入彈窗若只有一個主要確認動作，文字欄按 Enter 必須直接確認並關閉；自訂 overlay 使用文字欄 `onSubmit`，不可用會把按鈕移到主視窗工具列的 default-action shortcut。
- 需要和視窗或整體版面比較位置的物件，必須依父容器可用尺寸、比例與上下限約束計算；不可用固定像素 offset 模擬跨區位移。固定值只用於元件自身尺寸、間距與最小可讀邊界。
- 文件與實作不一致時，先記錄差異，再確認要更新產品意圖或程式行為。
- 角色、物品、時間軸與故事標籤的跨模組修改，需檢查正文引用、歷史定位與刪除行為。
- 產品版本（例如 V4.0.1）與 SwiftData schema／遷移版本分開描述。

## 完成工作後

- 更新 `docs/project-status.md` 的目前狀態與下一步。
- 若改變功能行為，更新對應規格與驗收條件。
- 若改變資料關係、刪除規則或匯出行為，重新檢查 `docs/consistency-audit.md`。
- 執行適用的測試或驗證，並記錄尚未驗證的範圍。
- 不要把待確認事項改寫成已決定的規格。
- 將值得長期保存的內容分流到規格、ADR、測試與專案狀態；不要永久堆在交接檔。
- 工作單元完成且沒有後續工作時，將 `docs/work-items/current.md` 與 `docs/handoffs/current.md` 標記為 `closed`；尚有下一步時維持 `active` 並留下單一明確起點。
