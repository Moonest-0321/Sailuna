# V9.2z 故事背景編解碼診斷

> 狀態：closed
>
> 工作單位：區分既有自由文字相容回退與疑似 JSON 解碼失敗，為後者及 JSON 編碼失敗加入診斷，保留原值和保存結果。

## 現況與邊界

- `StoryBackgroundContent` 將多欄位背景編成 JSON，沿用原本 `BookPlanningProfile.backgroundText` 字串；舊資料是自由文字，讀取時放入 `otherBackground`。
- JSON decode 失敗目前一律落入舊文字路徑；JSON encode 失敗目前回傳 `otherBackground`。本工作不改 schema、欄位格式、UI 或這兩個 fallback。
- 只在去除首尾空白後以 `{` 開頭的字串解碼失敗時記錯誤；普通舊文字的相容路徑不產生錯誤日誌。此判斷僅控制診斷，不改資料回傳。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | V9 已要求錯誤可診斷；使用者決定目前先記錄錯誤、不改操作結果。 |
| U | not_applicable | 無畫面變化。 |
| I | approved（限診斷） | 原自由文字仍轉為 `otherBackground`，編碼失敗仍回 `otherBackground`；只新增私有 OSLog。 |

## 完成條件

- 對疑似 JSON 解碼失敗及 JSON 編碼失敗加入分類清楚的日誌，不記背景原文或 payload。
- 正常 JSON、舊文字及失敗 fallback 均維持現有結果。
- 通過 `git diff --check` 與主機無簽章 Debug build；不新增或執行 XCTest。

## 結果

- `BookOutline.swift` 已將解碼與編碼 fallback 改成明確錯誤分支；只對以 `{` 開頭的疑似 JSON 記錄解碼錯誤，其他自由文字仍走原相容路徑。
- 日誌不含背景內容或 JSON payload，錯誤細節採 private；JSON 編碼與 UTF-8 轉換失敗仍回傳 `otherBackground`。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；`try?` 靜態命中由 19 處降至 17 處。未執行 XCTest、故障注入或 UI 驗收。
- 疑似 JSON 損壞時是否顯示修復入口尚未決定；本批只提供診斷。
