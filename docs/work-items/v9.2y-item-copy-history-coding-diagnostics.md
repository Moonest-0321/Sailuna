# V9.2y ItemCopyHistory JSON 編解碼診斷

> 狀態：closed
>
> 工作單位：為 `ItemCopyHistory` 的 JSON encode／decode fallback 記錄失敗診斷，但保留既有資料回傳與操作結果。

## 現況與目標

- `relatedCharacterIDsData` 將 `[UUID]` 編碼為 JSON bytes；目前 encode 失敗會回傳空 `Data`，decode 失敗會回傳空陣列。
- JSON 欄位已存在於目前 store，本批不改格式或 schema；只讓兩類 fallback 可由 OSLog 辨識。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已決定這類已盤點的錯誤先只記錄、不改既有結果；符合 V9 `try?` 錯誤可診斷規則。 |
| U | not_applicable | 不改畫面。 |
| I | approved（限診斷） | 只記 OSLog；encode 仍回空 `Data`，decode 仍回空陣列，不改 schema、格式或操作結果。 |

## 完成條件

- 兩個 `try?` 改成明確 `do/catch`，失敗時記錄分類清楚的 OSLog error，並回傳原本 fallback。
- 日誌不包含 JSON payload 或角色 ID 等使用者內容。
- 確認正常編解碼值不變；通過 `git diff --check` 與主機無簽章 Debug build。依開發者規則不新增／執行 XCTest。

## 結果

- `ItemCopyHistory` 的 encode／decode 已改為明確 `do/catch`；錯誤透過 `ItemCopyHistoryCoding` OSLog 記錄，錯誤細節採 private，訊息不帶 JSON payload 或 UUID。
- encode 仍回傳空 `Data`，decode 仍回傳空陣列，未改資料格式、schema 或操作結果。
- `git diff --check` 通過；主機無簽章 macOS Debug build 通過。
- `rg -n 'try\?' Sailune --glob '*.swift'` 靜態命中由 21 處降至 19 處。未執行 XCTest、故障注入或 UI 驗收。
- 壞資料是否應保留／呈現修復入口仍待相容性政策另議；本工作只增加診斷。
