# V9.1k 搜尋、設定與更多圖標語意接入

> 狀態：closed（九個入口已接入）
>
> 工作單位：讓語意明確的搜尋、設定與更多入口引用既有共用圖標表。

## 目標與完成條件

- 兩個既有「更多操作」入口均為 `ellipsis.circle`，將未被引用的 `.more` 定義對齊此字形並接入兩處。
- 四個 `magnifyingglass` 搜尋圖標、一個搜尋清除圖標及兩個設定入口引用 `.search`、`.clearSearch`、`.settings`；原字形與行為不變。
- 資料類型圖標 `gearshape.2`、AI 閱讀範圍 `doc.text.magnifyingglass`、放大／縮小等不同語意不納入。
- 差異檢查及主機 Debug build 通過。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能使用相同圖標；本批逐點核對既有入口語意。 |
| U | not_applicable | 畫面字形、位置、文字與互動不變。 |
| I | approved | 使用者已指示完成 V9；本批只替換已核對的字面值與未使用的 `.more` 定義。 |

## 驗證與下一步

- 2026-09-23：兩個「更多」、四個搜尋、一個清除搜尋及兩個設定入口已引用語意表；既有字形、文字及 action 均不變。
- 2026-09-23：`xmark.circle.fill` 另有事件卡片刪除語意，`gearshape.2` 是技術階段資料圖示，均保留現有實作。
- 2026-09-23：`git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
