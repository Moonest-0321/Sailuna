# V9.2w 時間軸大綱來源投影責任歸位

> 狀態：closed
>
> 工作單位：將時間軸事件來源選取使用的大綱排序與標題投影從大型 View 檔移入專責檔，保持 Store 讀取及呼叫行為。

## 範圍與完成條件

- 將 `TimelineOutlineSourceProjection` 從 `TimelineViews.swift` 移入 `TimelineOutlineSourceProjection.swift`。
- 保持 `orderedItems(book:planningStore:)`、`eventTitle(for:)` API、`@MainActor` 隔離、排序來源與標題截斷行為不變。
- 保持 `TimelineEventCreatePopover` 呼叫與 `V42OutlineTests` 既有直接測試引用不變。
- 不混入純日期投影，也不改 `PlanningRecordProjectionBuilder` 的資料查詢／錯誤政策。
- 比對搬移內容、確認原檔無重複型別；通過 `git diff --check` 與主機無簽章 Debug build。依開發者規則，不新增或執行 XCTest。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權完成 V9 全面內部程式規則整理；本批按明確責任拆分投影宣告。 |
| U | not_applicable | 不改畫面或互動。 |
| I | approved | 單純型別歸位，既有 API、資料查詢與呼叫端維持。 |

## 結果

- 2026-09-24：型別已移入 `TimelineOutlineSourceProjection.swift`，呼叫端及 `SailuneTests/V42OutlineTests.swift` 名稱未改。
- 2026-09-24：搬移宣告與 `HEAD:TimelineViews.swift` 原文逐字一致；來源檔無重複宣告。`git diff --check` 與主機無簽章 macOS Debug build 通過。
- 未新增或執行 XCTest；未做 GUI 驗收，因本批不改畫面與操作。
