# V9.1au 作者簡介兩處多行編輯器

> 狀態：active（兩個 ID 已接線並通過 Debug build，GUI／CJK／首行待驗收）；固定 editor ID：V9-0079、V9-0550。

- R：approved。使用者要求相同功能欄位共用程式碼及首行安全檢查；作者設定與首頁作者彈窗均編輯同一 `AuthorProfile.bio`。
- U：not_applicable。本批將既有 TextEditor、6 pt 邊框、各自 100／110 pt 高度與作者設定的手動 placeholder 封入同一元件，以參數保留兩畫面現況，不調整可見版面、內距或第一行。首行安全修正仍由 V9.1x／1am 的 UI 核定處理。
- I：approved。共用 `SailuneAuthorBioEditor` 只接兩個固定 ID，保留原 Binding set 時機；不得合併作者資料保存流程。
- 驗收：兩個欄位來源與顯示參數核對、無簽章 Debug build、`git diff --check`；100／110 pt、placeholder、CJK／首行／焦點實際畫面另驗收。

兩個來源已接入 `SailuneAuthorBioEditor`，原高度、6 pt 描邊、灰／primary 透明色、作者設定 placeholder 與 Binding 保留；主機無簽章 macOS Debug build 及 `git diff --check` 通過。
