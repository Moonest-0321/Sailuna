# V9.2a 參照投影與預覽排序責任歸位

> 狀態：closed（三個型別已歸位且 Debug build 通過）
>
> 工作單位：將已獨立使用、可按值驗證的參照掃描與排序型別移出大型 View 檔。

## 目標與完成條件

- 將 `WritingReferenceScanner`、`InspectorSectionNameMatcher` 移至 `WritingReferenceScanner.swift`；將 `CharacterDetailPreviewOrdering` 移至同名檔案。
- 保留三個型別的名稱、存取層級、方法內容及所有呼叫點；不改搜尋、排序、連結判斷或非同步取消行為。
- 新檔案由 Xcode 專案編入，現有 Swift 與既有測試呼叫點可正常編譯。

## 現況與範圍

- 三個型別目前位於 `InspectorViews.swift` 檔案開頭，分別負責正文參照掃描、設定欄位名稱比對與詳情預覽排序；`V5SettingViews.swift` 及既有 XCTest 亦直接使用這些型別。
- 變更只包含宣告搬移和必要 import。`InspectorLinkedEntry`、`CharacterReferenceSynchronizer` 與各 View 保留在原檔；跨 store 資料流程及 UI 互動不變。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 涵蓋內部程式責任與可維護性；本批只歸位三個既有獨立型別。 |
| U | not_applicable | 沒有畫面或互動變更。 |
| I | approved | 使用者已指示完成 V9；變更檔案、型別及驗證條件已限定。 |

## 驗證與下一步

- 2026-09-23：三個型別各只有一份宣告，原呼叫點與測試程式引用未改；`InspectorViews.swift` 原宣告已移除。
- 2026-09-23：首次 Debug build 發現 `NSAttributedString.Key.link` 需要在新檔明確引入 `AppKit`；補上後主機環境無簽章 Debug build 通過，`git diff --check` 通過。
- 本批未執行程式測試或 GUI 驗收；沒有修改演算法與資料流程。
- 內部資料錯誤處理候選另由 V9 規劃追蹤；本單不更動資料結果。
