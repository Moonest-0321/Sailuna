# V9.2ak 自訂封面讀取診斷

> 狀態：closed
>
> 工作單位：分辨無自訂封面與存在但無法讀取的封面檔，保留原預設封面 fallback。

## 範圍與非目標

- `BookCoverStore.pngData(for:)` 的 `try? Data(contentsOf:)` 目前將缺檔、讀取錯誤、空資料及無效圖片都回傳 `nil`。
- 檔案不存在仍直接回 `nil`；檔案存在但讀取失敗或內容無效時記錄 private OSLog，之後仍回 `nil`，由 `displayedCoverPNGData` 沿用預設封面。
- 不改圖片轉換、快取、匯出內容契約、資料目錄或使用者可見提示；日誌不含封面內容與路徑。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 內部錯誤處理規範與可維護性。 |
| U | not_applicable | 畫面與封面 fallback 不變。 |
| I | approved | 只將讀取錯誤轉為明確 catch 並記錄，回傳維持 `nil`。 |

## 完成條件

- 無封面不產生錯誤診斷；已有封面但讀取或解碼失敗可辨認。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；不新增或執行 XCTest。

## 結果與驗證

- 無自訂封面直接回 `nil`；檔案存在但讀取失敗或圖片資料無效時記錄 private OSLog，仍回 `nil`。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；`try?` 靜態命中由 10 降至 9。
- 未執行 XCTest、故障注入或實際匯出驗收。
