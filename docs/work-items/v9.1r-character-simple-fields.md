# V9.1r 角色區一般單行欄位接入

> 狀態：closed（五個欄位已接入）

## 工作單元

- 目標：讓角色區中五個純字串、原生圓角樣式的單行欄位改呼叫 `SailuneFormTextField`。
- 範圍：`AliasRow` 的備註、心理資料內容、角色關係類型與備註、角色事件標題。
- 非目標：別名名稱欄（有 `FocusState` 與提交副作用）、事件摘要（垂直多行）、關係歷史欄位及刪除／保存流程。
- 完成條件：五個欄位保留原標題、Binding、原生 `.roundedBorder` 和列內位置；完成來源核對、`git diff --check`、Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同欄位由統一程式碼承接；此批限定無特殊輸入修飾的角色區一般字串欄。 |
| U | not_applicable | 共用欄位內部仍使用原生 `TextField` 與 `.roundedBorder`；只替換等同組合，順序與 layout 不變。 |
| I | approved | 使用者已明確指示完成 V9；呼叫端只傳入既有 title 與 Binding。 |

## 驗證與限制

- 別名名稱欄保留原生 View，因其 `FocusState` 轉換時觸發專屬 rename action；不把該語意塞入一般欄位元件。
- 2026-09-24：五個呼叫點已接入 `SailuneFormTextField`；原 title、Binding 和 HStack 次序保留。
- 來源碼現有共用一般單行欄位十二處；功能檔精確 `\bTextField(` 靜態命中 92 處，`.roundedBorder` 靜態命中 55 處（統計含共用元件內實作）。
- 2026-09-24：`git diff --check` 與主機無簽章 macOS Debug build 通過；未新增或執行程式測試，GUI／VoiceOver 輸入尚未驗收。
