# V9.1ab 圓形新增圖標語意統一

> 狀態：closed
>
> 工作單位：統一相同新增語意的圓形 SF Symbol 呼叫，同時保留既有填色 variant 的差異。

## 範圍與完成條件

- 三個 `plus.circle` 呼叫改用 `SailuneSymbol.addCircle`：兩個新增事件入口、一個新增紀元入口。
- 三個 `plus.circle.fill` 呼叫改用 `SailuneSymbol.addCircleFilled`：新增角色列、關係新增選單、時間軸新增選單。
- 保留各自圖形、文字、menu／button、action、style、位置與顏色。
- 原始兩種 SF Symbol 字串只由共用表提供；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標及程式呼叫統一；兩種圓形符號皆表示新增。 |
| U | not_applicable | 保留既有 filled／outline 變體與所有可見呈現。 |
| I | approved | 屬已授權 V9 語意資源採用，僅將相同符號來源集中。 |

## 已完成

- 新增 `SailuneSymbol.addCircle` → `plus.circle` 與 `SailuneSymbol.addCircleFilled` → `plus.circle.fill`。
- `InspectorViews.swift` 兩處及 `TimelineViews.swift` 四處完成接入。
- 未改資料或使用者可見行為；未執行 XCTest 或 GUI 驗收。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
