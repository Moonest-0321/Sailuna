# V9.2ad 側邊欄與勢力畫面責任歸位

> 狀態：closed
>
> 工作單位：將 V5 設定畫面中最後兩組不同功能的 View 歸入各自檔案。

## 現況與範圍

- `V5SettingViews.swift` 目前只保管 `SidebarSettingsManagerView` 與四個勢力相關 View；地點與世界條目已由 V9.2ac 搬移。
- 本批從目前工作樹保留宣告正文，將側邊欄管理畫面放入 `SidebarSettingsManagerView.swift`，勢力清單、層級、詳情及關係編輯放入 `PowerSettingViews.swift`。
- 不改文字、圖標、畫面位置、Binding、Store 操作、保存／刪除流程、schema 或資料。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權 V9 內部程式責任整理，兩組畫面各有明確領域責任。 |
| U | not_applicable | 僅搬移宣告，不改 UI。 |
| I | approved | 以目前工作樹為搬移來源，保持 module 可見名稱與私有勢力編輯器作用域；新檔只加入必要 import。 |

## 完成條件

- 五個 View 在新檔各宣告一次；原 `InspectorViews.swift` 呼叫點不變。
- 搬移正文與搬移前工作樹一致，檔案責任在架構及採用矩陣記錄。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 側邊欄管理宣告 78 行搬至 `SidebarSettingsManagerView.swift`，SHA-256 `e87fd0c17e53b1a21d00ab0bb47402811dd304bfbe8e2054dd8cd5f928fb7c61`；勢力宣告 910 行搬至 `PowerSettingViews.swift`，SHA-256 `16fae01d59929982ae03a8bdba66bcd6d210e4d6ea047b515b0c74872bccefbd`。兩段直接取自搬移前工作樹，只有新檔重複加入原有 import。
- 五個 View 各僅宣告一次，`InspectorViews.swift` 呼叫點維持原名。原 `V5SettingViews.swift` 已由這兩個具名檔取代。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。首次沙箱內建置因 Xcode SwiftData macro 外掛無法啟動失敗；以允許外掛啟動的同一建置命令重跑後通過。未執行 XCTest 或 GUI 驗收。
