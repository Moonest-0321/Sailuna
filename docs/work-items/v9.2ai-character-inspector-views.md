# V9.2ai 角色 Inspector 畫面責任歸位

> 狀態：closed
>
> 工作單位：將角色清單、詳情與輔助畫面從 Inspector 根導覽檔歸入角色專責檔。

## 現況與範圍

- `InspectorViews.swift` 的根導覽後方，自 `CharacterListContainerView` 起是角色清單、列、詳情、勢力成員、參照、區段容器及出生日期 Picker 的連續區塊。
- 本批將此區塊逐字搬至 `CharacterInspectorViews.swift`，保留已具 module 可見性的入口；檔案私有輔助 View 仍保持 `private`。
- 根導覽和 `InspectorLinkedItemsRow` 留在 `InspectorViews.swift`，其呼叫與 route 狀態不變。
- 不改角色資料、正文引用、歷史定位、刪除、UI 排列／文字／圖標、schema 或保存政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者授權 V9 內部責任整理；角色清單與詳情有獨立於 Inspector 根導覽的責任。 |
| U | not_applicable | 僅搬移宣告，不改畫面呈現或互動。 |
| I | approved | 逐字搬移目前工作樹的角色區塊，只加入新檔必要 import。 |

## 完成條件

- 角色區塊各 View 只宣告一次，根導覽原呼叫可編譯。
- 搬移段落與搬移前目前工作樹完全一致，角色操作與資料相容性不變。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 角色區塊 1021 行已從目前工作樹逐字搬至 `CharacterInspectorViews.swift`；搬移段落 SHA-256 前後同為 `5e472765b2e7efaee608c80d9c12e778c734397b0a19c0628458db842a14bee2`。
- 九個 View 各只在新檔宣告一次；`InspectorRootView` 保留原導覽呼叫。`InspectorViews.swift` 現為 422 行，管理共用連結列、route 與根導覽。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；未執行 XCTest 或角色畫面 GUI 驗收。
