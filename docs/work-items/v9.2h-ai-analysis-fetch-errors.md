# V9.2h AI 設定分析查詢錯誤傳遞

> 狀態：closed（七處主 store 查詢失敗不再折疊為空資料）
>
> 工作單位：讓既有設定分析在組裝物品、能力、勢力快照時，遇到 SwiftData 查詢錯誤即停止送出附件。

## 目標與完成條件

- `itemSnapshot` 的歷史 Node、`abilitySnapshot` 的等級角色與歷史 Node，以及 `powerSnapshot` 的角色、物品、能力、Node 共七處查詢改為可拋錯。
- 保留成功查詢時原有篩選、排序、附件文字與指定維度；真正的空資料仍可產生原本的空段落。
- 錯誤經 `settingAnalysisAttachment` 傳至 `EditorWorkspaceView` 既有 `catch`，由 `aiChatModel.reject(error.localizedDescription)` 顯示，停止 `sendValidated`。
- `V5SettingsStore` 的列表 API 仍可能把讀取失敗折疊為空清單；本批不宣稱整個設定分析已具完整錯誤傳遞。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求完成 V9 內部程式規範；此為已盤點的單一附件組裝鏈中七個明確吞錯點。 |
| U | not_applicable | 沿用現有 AI 錯誤出口，不調整控制項、版面或文案。 |
| I | approved | 使用者已授權 V9 實作；修改限於快照函式的 throws 傳遞，無資料格式或 schema 變更。 |

## 驗證與限制

- 2026-09-24：七處 `try? context.fetch` 改為 `try context.fetch`，成功資料轉換邏輯不變。
- `git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試、故障注入或 GUI 驗收。
- 勢力的 `V5SettingsStore` 讀取及附件之外的資料選擇流程需另行稽核；不能由此工作推論整個 AI 分析完整可靠。
