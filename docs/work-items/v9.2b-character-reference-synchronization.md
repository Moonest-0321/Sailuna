# V9.2b 角色參照同步責任歸位

> 狀態：closed（同步責任已歸位且 Debug build 通過）
>
> 工作單位：將角色名稱變更時的正文參照同步流程移出大型 View 檔。

## 目標與完成條件

- 將 `CharacterReferenceSynchronizer` 和其候選資料 `UnlinkedReferenceCandidate` 移到 `CharacterReferenceSynchronizer.swift`。
- `InspectorViews.swift` 保留 `UnlinkedReferenceReviewView` 與使用者操作流程；同步型別繼續負責候選掃描、套用、已連結名稱更新、保存與通知。
- 方法內容、呼叫點、主 actor 隔離、取消檢查、保存與通知順序都不改；跨檔使用所需的型別存取層級由 `private` 改為模組內可見。
- 確認各型別只宣告一次、原呼叫點可編譯，並檢查差異範圍。

## 範圍與非目標

- 只移動既有型別及加入新檔必要 imports；不改正文連結格式、文字替換規則、使用者審核畫面、SwiftData schema 或資料修復行為。
- 這批不處理先前稽核的跨 store 建立／地圖查詢錯誤流程；它們需要獨立驗收。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 規範內部責任邊界；本單只將非 View 的既有同步流程歸位。 |
| U | not_applicable | 操作畫面、文字與互動完全沿用原實作。 |
| I | approved | 使用者已指示完成 V9；本單的型別、檔案和不變條件已限定。 |

## 驗證與下一步

- 2026-09-23：`CharacterReferenceSynchronizer` 與 `UnlinkedReferenceCandidate` 已移到同名檔案，皆只有一份宣告；`UnlinkedReferenceReviewView` 留在 `InspectorViews.swift`，原呼叫點未改。
- 2026-09-23：兩個型別因跨檔使用改為模組內可見；方法本文、`@MainActor`、保存與通知順序均維持原樣。`git diff --check` 與主機環境無簽章 Debug build 通過。
- 未執行程式測試或 GUI 驗收；本批沒有新增功能行為。
