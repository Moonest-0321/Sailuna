# V9.2k 啟動資料目錄建立錯誤

> 狀態：closed（目錄建立錯誤進入啟動失敗出口）
>
> 工作單位：建立主資料 store 目錄失敗時，讓 App 在啟動階段回報原始檔案錯誤，避免延後成難以判讀的資料庫開啟失敗。

## 完成條件

- `storeURL` 保留既有正式路徑與 DEBUG 測試覆寫／隔離選擇，只計算路徑。
- `makeModelContainer()` 在還原與打開任何 store 前確認主 store 父目錄可建立；錯誤以既有 `StartupStageError` 包裝並進入 `.failed` 畫面。
- 不變更 store 檔名、schema、遷移、備份位置或正常啟動流程。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求完成 V9 內部程式錯誤規範；本處 `try?` 是已盤點的啟動資料目錄重要操作。 |
| U | not_applicable | 沿用既有啟動失敗畫面，只在錯誤時提供更準確的階段與原因。 |
| I | approved | 使用者已指示完成 V9；修改限於目錄建立責任轉移與錯誤傳遞。 |

## 驗證與限制

- 2026-09-24：正式與 DEBUG 路徑分支保留；`makeModelContainer()` 在備份還原與 store 開啟前建立其父目錄，失敗以 `StartupStageError` 包裝。
- `git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或實際破壞檔案系統的故障注入。
- 備份還原 rollback 為獨立 V9.2g 工作，與本批資料目錄檢查無關。
