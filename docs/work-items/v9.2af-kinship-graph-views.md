# V9.2af 親屬關係圖畫面責任歸位

> 狀態：closed
>
> 工作單位：將親屬關係圖與新增血緣關係 sheet 從 `InspectorViews.swift` 移至專責檔。

## 現況、範圍與非目標

- `InspectorViews.swift` 尾段包含 `KinshipGraphView` 和 `AddKinshipSheet`，分別負責親屬圖／列表呈現與關係建立；角色導覽只呼叫圖畫面。
- 本批從目前工作樹逐字搬移這兩個 module 可見 View 至 `KinshipGraphViews.swift`，保持原資料查詢、選取、縮放、建立關係與關閉流程。
- 不改 UI 文字、圖標、大小、位置、操作、資料關係、正文引用、刪除、schema 或保存政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權 V9 內部責任整理；親屬關係圖和建立 sheet 構成獨立畫面責任。 |
| U | not_applicable | 僅搬移宣告，不改畫面。 |
| I | approved | 搬移目前工作樹的完整尾段，保持呼叫名稱與 View 本文，只補新檔 import。 |

## 完成條件

- 兩個 View 各僅宣告一次，`InspectorRootView` 呼叫及 sheet 呼叫保持原名。
- 搬移段落與搬移前目前工作樹一致；`InspectorViews.swift` 保留角色詳情與出生日期欄位。
- `git diff --check` 和主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 兩個 View 的 284 行尾段已從搬移前目前工作樹逐字移至 `KinshipGraphViews.swift`；段落 SHA-256 `9f3a2ea859d390b6d8d1da88ec5d1ce9ed56270fdc507ae0d3bee100db3a2ef6`。
- `KinshipGraphView` 與 `AddKinshipSheet` 各僅在新檔宣告一次；原檔保留 `BirthDatePickerSection` 及上游角色畫面。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。未執行 XCTest 或親屬關係圖 GUI 驗收。
