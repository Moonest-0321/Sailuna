# V9.2x 時間軸年號管理畫面責任歸位

> 狀態：closed
>
> 工作單位：將年號管理、年號列、改元與年號編輯 popover 從大型時間軸 View 檔拆至同功能專責檔。

## 目標與範圍

- 搬移 `EraManagerPopover`、`EraRow`、`EraChangePopover`、`EraEditPopover` 與其專用 `eraPalette` 到 `TimelineEraManagementViews.swift`。
- 只將跨檔呼叫的三個 popover 從 `private` 調整為 module-internal；`EraRow` 與色票仍限於新檔。
- 保持既有 initializer、`TimelinePanelView` 呼叫點、ModelContext／Store 依賴、保存／刪除順序、確認文字及 UI modifiers。
- 搬移來源為當前工作樹，因此沿用先前 V9 已完成的新增／完成／取消／刪除文案與圖標 key；本切片不新增其他可見改動。
- `Color(hex:)` 是多功能共用轉換，留在原檔；不混入本次年號 View 搬移。

## 非目標

- 不改年號／時間軸資料模型、schema、色票、排序、刪除後修復、改元演算法或錯誤處理政策。
- 不調整畫面佈局、文案、互動或控制項樣式。
- 不新增或執行 XCTest；以原始碼差異核對、`git diff --check` 與 Debug build 驗證。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權完成 V9 內部程式整理；本批按領域責任拆分時間軸年號管理 View。 |
| U | not_applicable | 只歸位宣告與所需存取層級，不改可見呈現或操作。 |
| I | approved | 原呼叫與領域操作維持，只有跨檔 View 存取層級由 file-private 調整為 module-internal。 |

## 驗收與風險

1. 搬移後新檔只有上述四個 View 與其專用色票；`TimelineViews.swift` 不再重複宣告。
2. 三個 popover 在原 `TimelinePanelView` modifier 位置及參數下建置成功。
3. `Color(hex:)` 保持原檔且全專案原呼叫可解析；不改 `CrossStoreDeletionCoordinator`、年號操作與資料。
4. 通過 `git diff --check` 及主機無簽章 macOS Debug build；不宣稱 GUI 或故障情境驗收。

## 結果

- 已將年號管理、年號列、改元與編輯 popover 移至 `TimelineEraManagementViews.swift`；原檔無重複宣告。跨檔呼叫的三個 popover 改為 module-internal，列與色票維持 file-private。
- `Color(hex:)`、時間軸 popover 呼叫點、Store 依賴與資料操作流程維持。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。未新增或執行 XCTest，也未做 GUI 驗收。
