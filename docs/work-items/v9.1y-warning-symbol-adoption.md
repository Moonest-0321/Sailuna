# V9.1y 警告圖標語意統一

> 狀態：closed
>
> 工作單位：讓同一警告語意引用共用圖標來源，不改既有文案、顏色、顯示條件或行為。

## 範圍與完成條件

- 將 V9 靜態盤點中三個 `exclamationmark.triangle.fill` 呼叫點改用 `SailuneSymbol.warning`。
- 保留每處原 Label／Image、繁體中文、色彩、條件與 action。
- 驗證這三個畫面呼叫均使用語意 key，SF Symbol 字串只由共用表提供；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 統一相同功能圖標與程式呼叫；本切片只處理來源碼集中。 |
| U | not_applicable | 圖標、文案、顏色、條件與位置均維持原值，不改畫面呈現。 |
| I | approved | 屬已授權的 V9 共用圖標採用，僅將相同語意呼叫改讀共用 key。 |

## 已完成

- `SailuneSymbol.warning` 對應原 `exclamationmark.triangle.fill`。
- `OutlineViews.swift` 的「來源已刪除」、「需要調整狀態」及 `InspectorViews.swift` 的參照錯誤提示共三處已改引用該 key。
- 未新增、修改或刪除使用者資料；未改畫面及操作行為。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。未執行 XCTest 或 GUI 驗收。
