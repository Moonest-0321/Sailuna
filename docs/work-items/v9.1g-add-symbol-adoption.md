# V9.1g 新增與放大圖標語意接入

> 狀態：closed（34 個畫面呼叫點已接入語意圖標）
>
> 工作單位：將既有 `plus` 圖形依操作語意接入集中圖標表。

## 目標與完成條件

- 2026-09-23 初篩：Swift 原始碼共有 35 個 `"plus"` 字面值，其中一個是 `SailuneSymbol` 定義；其餘 34 個畫面呼叫點以 action 分類。
- 新增書、卷節、物品、能力、關係、事件、地圖項目或對話內容的操作引用 `.add`；地圖放大 25% 的控制引用 `.zoomIn`。
- 只替換圖標資源來源；原圖形、文字、位置、button style、命中區、快捷鍵與 action 不變。
- 確認 raw 字面值只留在語意表，並通過靜態差異檢查與 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標一致，且不同功能須依語意統一呼叫。 |
| U | not_applicable | 原 `plus` 字形與畫面行為保持不變。 |
| I | approved | 使用者已指示完成 V9；本批限定同一字形、兩種已由程式 action 確認的操作。 |

## 驗證與下一步

- 2026-09-23：33 個原始新增呼叫點改用 `.add`，地圖放大一處改用 `.zoomIn`；連同前一批已接入的新增 Label，現在共有 34 個 `.add` 呼叫點。原 raw `"plus"` 只留在 `SailuneSymbol` 定義。
- 2026-09-23：逐項確認資源表仍回傳 `plus`，`git diff --check` 與主機環境無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
