# V9.1z 時間序圖標語意統一

> 狀態：closed
>
> 工作單位：集中表示時間軸／時間序的共用 SF Symbol，不改原圖形及各處互動。

## 範圍與完成條件

- 將角色歷史「時間序版本」入口與時間軸兩種版面「尚無時間記錄」空狀態的三處 `clock` 改用 `SailuneSymbol.timeline`。
- 保留原有 `clock` 字形、按鈕文字、accessibility label、空狀態文字、操作及版位。
- 驗證以上三個呼叫點使用同一語意 key，原始字串只由共用語意表定義；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能／領域圖標與程式呼叫統一；本切片集中三個表示時間序的同圖形來源。 |
| U | not_applicable | 可見圖形及位置不變，操作與文案不變。 |
| I | approved | 屬已授權 V9 語意資源採用，僅更改圖標來源。 |

## 已完成

- `SailuneSymbol.timeline` 對應原 `clock`。
- `CharacterHistoryViews.swift` 一處、`TimelineViews.swift` 兩處均已引用此 key。
- 未變更資料或操作；未執行 XCTest 或 GUI 驗收。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
