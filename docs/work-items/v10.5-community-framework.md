# V10.5 社群介面骨架

> 狀態：implementation_complete_gui_pending
>
> R：使用者回覆「R」核准。U：使用者回覆「U」核准上述版面。I：使用者先前明確要求依指定內容加搜尋框並先做論壇框架，授權直接實作。
>
> 範圍：搜尋模板頁上方輸入框；論壇頂欄橫向分類導覽與空內容頁面骨架；我的模板刪除操作。

## 已決定

- 「搜尋模板」分頁在分頁列下方顯示共用搜尋欄，提示文字為「搜尋模板」。
- 本階段只建立輸入框外觀，不接搜尋目錄、網路服務或搜尋結果；使用者要求先照指定呈現搜尋框。
- 論壇先呈現五個討論區：官方公告、寫作交流、作品交流、帆夢交流、功能建議。
- 選擇討論區可切換其標題與空內容狀態；不建立假貼文。
- 我的模板卡片提供刪除按鈕；刪除前確認，只移除本機模板檔，不影響來源書或已套用建立的書籍。
- 帳號、登入、發文、回覆、公開資料服務與內容管理延後處理。

## UI 骨架

- 論壇主頁頂欄橫向排列五個討論區按鈕，內容區顯示目前討論區與空內容狀態。
- 模板搜尋框置於「搜尋模板」頁面的上方、分頁列下方；離開該分頁時不顯示。
- 沿用既有共用搜尋欄與 theme，不新增圖標語意或資料模型。

## 驗收條件

- 切到「搜尋模板」時可見上方搜尋框；切回「我的模板」時隱藏。
- 論壇頂欄五個討論區均可選取；下方標題隨選取項目切換，空資料時不出現示例貼文。
- 我的模板刪除按鈕要求確認；確認後卡片消失，取消時模板保留。
- 不新增登入、資料庫、網路請求、發文或回覆流程。

## 實作紀錄

- 已完成模板搜尋頁上方搜尋欄、論壇頂欄橫向分類骨架，以及我的模板刪除按鈕／確認流程；受影響 Swift 檔案 frontend parse、git diff --check 與主機環境無簽章 Debug build 通過。未執行 XCTest。隔離 Debug app 啟動成功並只建立 /private/tmp 隔離 stores；System Events Apple Event 被 macOS 拒絕（-1743），螢幕擷取無法建立顯示畫面，因此 GUI 點選驗收未完成。正式作者資料未操作。

## 使用者版面修正（U 更新）

- 使用者要求論壇分類改為頂欄橫向排列，模板搜尋框移到上方；已依指示調整。

- 使用者於 2026-09-26 要求分類改為頂欄橫排、搜尋框移到上方；修正後 Debug build 通過。

- 使用者追加要求：在「我的模板」提供刪除按鈕；採確認後移除單一本機模板，錯誤沿用模板操作提示。

## GUI 驗收阻礙

- 已用全新 bundle ID 啟動隔離 Debug app；只在 /private/tmp/SailuneV105Isolation 建立測試 stores。
- macOS 拒絕本工作階段對 System Events 傳送 Apple Event，screencapture 回報無法從 display 建立影像。無法進行點選或視覺驗收；需在有 GUI／Automation 權限的環境檢查搜尋框位置、論壇分類切換及模板刪除確認／取消。
