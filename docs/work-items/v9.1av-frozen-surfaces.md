# V9.1av 固定清單中的重複背景來源

> 狀態：active（34 個 ID 已接線、Debug build 通過，Light／Dark／hover 待驗收）；固定 surface ID：10 個 controlBackground、8 個 windowBackground、6 個 0.05 次要色命中，以及 10 個相同用途的邊框／0.04 卡片命中，共 34 個 ID、27 個實際色值位置。

- R：approved。使用者要求背景顏色等重複性質由統一程式碼提供；此批只處理凍結 ID 中相同的 NSColor／低對比列色。
- U：not_applicable。`SailuneTheme.controlSurface`、`windowSurface`、`insetRowSurface`、`faintCardSurface`、`characterSectionBorder`、`outlinePlaceholderBorder` 與原色值完全相同，保留原 shape、opacity、位置及互動。
- I：approved。只替換固定清單中對應原色值；不同用途的其他 `0.08`、accent、state、stroke 需逐 ID 核對，不因數值相同合併。
- 驗收：34 個 ID 的現行來源及色值映射、無簽章 Debug build、`git diff --check`；Light／Dark／hover／hit-test 實際畫面待驗收。

27 個現行色值表達式均已改用同值 token；多個重複 regex 命中對在複核表分別保留 ID。無簽章 macOS Debug build 與 `git diff --check` 通過。
