# V9.1ad 匯出 TXT 圖標語意統一

> 狀態：closed
>
> 工作單位：統一書籍總覽與編輯工作區兩個相同「匯出 TXT」操作的圖標來源。

## 範圍與完成條件

- 將兩處匯出標籤的 `doc.text` 改用 `SailuneSymbol.exportText`。
- 保留 Label 文案、ExportManager 呼叫、menu 結構、檔名與匯出內容。
- 三處其他 `doc.text` 代表節標記或未選取節的空狀態，不在本批改動。
- 驗證兩個操作使用 semantic key；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求同功能圖標與程式呼叫統一；兩處均為匯出 TXT。 |
| U | not_applicable | 圖形、文字、功能與位置均不變。 |
| I | approved | 屬已授權 V9 語意資源採用，只集中 SF Symbol 來源。 |

## 已完成

- 新增 `SailuneSymbol.exportText`，對應原 `doc.text`。
- `BookOverviewView.swift`、`EditorWorkspaceView.swift` 的兩個 TXT 匯出入口已接入。
- 未改匯出內容或使用者資料；未執行 XCTest 或 GUI 驗收。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
