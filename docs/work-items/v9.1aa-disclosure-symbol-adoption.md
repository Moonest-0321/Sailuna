# V9.1aa 導覽展開圖標語意統一

> 狀態：closed
>
> 工作單位：將表示列／卡片可進入下一層的重複圖標集中為語意資源，保留不同用途的同字形例外。

## 範圍與完成條件

- 將物品副本列、能力等級列、故事背景導覽卡及角色物品列四個 `chevron.right` 呼叫改用 `SailuneSymbol.disclosure`。
- 保留原圖形、文字、色彩、action、命中區與位置。
- 時間軸末端表示時間延續的第五個 `chevron.right` 不納入本批，因為它不是導覽 disclosure。
- 確認四個導覽位置均使用 semantic key；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標與程式呼叫統一；四處均表示進入對應內容。 |
| U | not_applicable | 圖標、位置、文字及互動均保持原狀。 |
| I | approved | 屬已授權 V9 圖標採用；只將圖標來源改為共用語意 key。 |

## 已完成

- 新增 `SailuneSymbol.disclosure`，對應原 `chevron.right`。
- `CharacterSectionViews.swift`、`BookOverviewView.swift`、`InspectorViews.swift` 共四處已接入。
- `TimelineViews.swift` 時間延續指示保留直接字串，作為明確語意例外。
- 未改資料與使用者可見行為；未執行 XCTest 或 GUI 驗收。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
