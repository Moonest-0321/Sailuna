# V9.1i 移除與選取圖標語意接入

> 狀態：closed（12 個圖標呼叫點已依語意接入）
>
> 工作單位：分類既有 `minus.circle`／`checkmark` 圖形並統一引用語意表。

## 目標與完成條件

- 八個設定集 `minus.circle` 呼叫點依資料 action 區分解除關聯 `.removeAssociation` 與移除獨立紀錄 `.removeEntry`；兩者保持同一原字形。
- 四個 `checkmark` 呼叫點依實際作用區分確認改名 `.confirm` 與顯示目前選取 `.selected`，保持同一原字形。
- 不更改按鈕位置、role、文字、命中區、保存、刪除規則或選取狀態。
- 確認 raw 字面值只留在語意表，並通過差異檢查及 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標一致，且共用圖標資源需明確區分不同操作。 |
| U | not_applicable | 字形及所有可見畫面、互動保持原值。 |
| I | approved | 使用者已指示完成 V9；本批限定兩個既有字形與 12 個已核對呼叫點。 |

## 驗證與下一步

- 2026-09-23：八個 `minus.circle` 分為五個 `.removeAssociation`、三個 `.removeEntry`；四個 `checkmark` 分為兩個 `.confirm`、兩個 `.selected`。原字面值只留在 `SailuneSymbol` 定義。
- 2026-09-23：原字形與 action 未改，`git diff --check` 和主機環境無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
