# V9.1d 刪除圖標語意資源接入

> 狀態：closed（35 個畫面呼叫點已接入語意圖標）
>
> 工作單位：讓既有刪除操作透過同一圖標語意表取得原有圖形。

## 目標與完成條件

- 盤點 `"trash"` 呼叫點，讓資料刪除使用 `SailuneSymbol.delete`；「移除持有人」與「移除封面」保留不同語意 key，圖形仍維持 `trash`。
- 只替換 `systemName`／`systemImage` 的來源，保留原顯示文字、tooltip、accessibility label、位置、按鈕 role、action 和資料操作。
- `AppearanceRow` 的刪除按鈕保留現況，待其獨立工作單 U／I 關卡完成後再接入。
- 確認呼叫點數量、剩餘原始 `"trash"` 位置、差異與 Debug build。

## 現況與範圍

- 2026-09-23 靜態搜尋：`Sailune/**/*.swift` 共有 37 個 `"trash"` 字面值，其中一個是 `SailuneSymbol` 自身對照，36 個是畫面呼叫點。
- 既有圖形相同但語意不同：角色持有人移除後副本保留；封面移除會清除自訂封面；其餘屬資料或功能項刪除。兩個例外各自使用具名 key，不用一般 `.delete` 掩蓋其影響。
- 不涉及圖形變更或一般刪除／移除文案統一；文案需依每個領域的刪除影響另行核對。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者明確要求相同功能使用相同圖標，並要求統一程式碼呼叫。 |
| U | not_applicable | 原 SF Symbol 字形、按鈕位置與互動全部保持；`AppearanceRow` 的 UI 試行另案管理。 |
| I | approved | 使用者已指示完成 V9；本批限定單一圖標家族及語意例外。 |

## 驗證與下一步

- 2026-09-23：33 個資料刪除操作改用 `SailuneSymbol.delete.systemName`；移除持有人與移除封面各一處使用 `.removeHolder`／`.removeCover`。36 個原畫面呼叫點中，唯一剩下的 raw `"trash"` 是 U／I 尚未批准的 `AppearanceRow`；另一個字面值位於語意表自身。
- 2026-09-23：逐項核對原 SF Symbol 均為 `trash`，未改文字、tooltip、role、action 或位置。`git diff --check` 與主機環境無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
