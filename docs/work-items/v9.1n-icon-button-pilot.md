# V9.1n 28 pt 純圖標按鈕共用試行

> 狀態：closed（共用按鈕已由一個既有呼叫點採用）
>
> 工作單位：將書籍背景彈窗既有 28 × 28 pt 關閉按鈕的圖標、命中範圍、tooltip 與輔助使用名稱集中為可重用控制項。

## 現況與完成條件

- `BookOverviewView` 的「關閉故事背景」按鈕已有 `.close` 圖標、28 × 28 pt Image frame、矩形 content shape、`.plain` style、相同的 help／accessibility label 及原 closure。
- 共用 `SailuneIconButton` 承接上述組合，以 `SailuneLayout.iconHitSize` 管理既有 28 pt 值，呼叫端只提供語意圖標、描述文字、角色及 action。
- 只替換此一呼叫點；畫面位置、尺寸、樣式、關閉動作與中文提示不變。`AppearanceRow` 的刪除按鈕仍待獨立 U／I，不能由本工作代為核定。
- `SailuneActionCopy` 的書籍背景關閉描述與 String Catalog 採相同既有繁體中文值。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求按鈕／觸及規則與相同功能的圖標、描述文字集中；此處是既有 28 pt 組合的精確歸位。 |
| U | not_applicable | 本批保留原有 28 pt frame、content shape、style、位置、提示及可見圖形；不新增 UI 行為。 |
| I | approved | 使用者已指示完成 V9；實作限於共用控制宣告與此一現有呼叫點。 |

## 驗證與限制

- 2026-09-24：`BookOverviewView` 的既有 Image 28 × 28 pt frame、Rectangle content shape、`.plain` style、help／accessibility label 及關閉 closure 已由 `SailuneIconButton` 承接；文字 key 維持「關閉故事背景」。
- `git diff --check`、String Catalog JSON 語法檢查及主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
- 沒有實際 App 畫面時，建置與來源碼比對不能證明 Button 真正命中範圍或 Light／Dark 呈現；`AppearanceRow` U 仍 pending。
