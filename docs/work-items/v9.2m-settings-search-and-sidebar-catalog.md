# V9.2m 設定搜尋與側邊欄目錄責任歸位

> 狀態：closed（兩組宣告已原樣歸位）
>
> 工作單位：將 `V5SettingsSearch` 純搜尋投影及 `SidebarSettingCatalog` 的預設列／選取規則分別移出 `V5SettingModels.swift`。

## 目標與完成條件

- `V5SettingsSearch` 搬入專責搜尋檔；地點、世界條目欄位、trim、localized case-insensitive 比對與空查詢回傳原陣列的規則不變。
- `SidebarSettingCatalog` 搬入專責目錄檔；revision、預設列建立、V1 升級時 worldTerm 可見性、保存與排序／選取規則不變。
- 兩段正文逐字搬移，Store 與 View 呼叫點不變；歷史 settings schema／migration snapshot 不動。
- 通過差異檢查與主機 Debug build；不以拆檔本身當作資料流程驗收。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 讓內部規則與新增功能更容易維護；這兩組具名責任與 schema 宣告有清楚邊界。 |
| U | not_applicable | 原函式、回傳值與呼叫點均保留，無 UI 變更。 |
| I | approved | 使用者已指示完成 V9；本批限於原樣移動這兩組宣告。 |

## 驗證與限制

- 2026-09-24：`V5SettingsSearch` 與 `SidebarSettingCatalog` 正文分別與 `HEAD:V5SettingModels.swift` 對應區塊逐字一致；原檔已無重複宣告，Store／View 呼叫點不變。
- `git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
- `SidebarSettingCatalog` 仍在同一 settings store 保存，沒有變更跨 store 邊界或 schema。
