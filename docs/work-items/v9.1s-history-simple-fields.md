# V9.1s 角色歷史一般單行欄位接入

> 狀態：closed（五個欄位已接入）

## 工作單元

- 目標：將能力階段、物品與關係歷史中的五個一般文字欄位統一呼叫 `SailuneFormTextField`。
- 範圍：`AbilityHistoryRow` 的階段與描述、`ItemHistoryRow` 的自由文字、`RelationshipHistoryRow` 的關係與備註。
- 非目標：日期數值欄（有數字過濾與固定尺寸）、時間定位控制、刪除按鈕及歷史保存／新增流程。
- 完成條件：五個欄位保留 title、Binding 與行內位置；`onChange` 時間戳更新不改；來源核對、`git diff --check` 與 Debug build 通過。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同欄位由統一程式碼承接；本批只處理一般字串欄位。 |
| U | not_applicable | 共用元件維持原生 `TextField`／`.roundedBorder`；不改列順序、寬度或間距。 |
| I | approved | 使用者已指示完成 V9；本批只替換樣式相同的欄位呼叫。 |

## 驗證與限制

- 數值欄與歷史時間定位仍使用專用元件，保留數值過濾、日期選取和固定尺寸行為。
- 2026-09-24：五個欄位已接入 `SailuneFormTextField`；原 title、Binding、列順序及時間戳更新 modifier 保留。
- 專案現有共用一般單行欄位 17 處，精確 `\bTextField(` 靜態命中 87 處，`.roundedBorder` 靜態命中 50 處（均包含共用元件內實作）。
- 2026-09-24：`git diff --check` 與主機無簽章 macOS Debug build 通過；未新增或執行程式測試，GUI、CJK 輸入與 VoiceOver 仍待整體 UI 驗收。
