# V9.1as 固定清單重複語意圖標

> 狀態：active（18 個 ID 已接線，Debug build 通過；Light／Dark／VoiceOver 待驗收）；固定 26 個 symbol ID：V9-0112、0546、0194、0666、0356、0726、0614、0733、0978、0989、0097、1400、0721、1396、1325、1412、1404、1913。

- R：approved。使用者要求同功能／領域的圖標由同一程式碼提供；這 26 個固定點以現有字形表示相同的圖片、重排把手、連結、設定側欄、地圖堆疊、關聯角色、列導覽、節文件或四類領域。
- U：not_applicable。本批只將原 `systemImage`／`systemName` 字串換為同字形的語意 key；圖標、文字、位置、動作、樣式與命中不變。
- I：approved。於 `SailuneSymbol` 增加具名 key 並只接入上述固定 ID。上下移動箭頭及「新增副本」的不同字形不在本批，仍依原 U 待核定工作單處理。
- 驗收：26 個 ID 逐項核對 key 與原字形，Debug build、`git diff --check`；實際 Light／Dark、hover、VoiceOver 另驗收。

26 個固定 ID 的原字形與現行語意 key 已逐項核對；主機無簽章 macOS Debug build 與 `git diff --check` 通過。
