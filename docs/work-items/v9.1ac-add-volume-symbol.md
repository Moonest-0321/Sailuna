# V9.1ac 新增卷圖標語意統一

> 狀態：closed
>
> 工作單位：集中三個相同「新增卷」操作的 SF Symbol 來源，保留所有既有介面行為。

## 範圍與完成條件

- 將書籍總覽、卷 context menu、編輯工作區三處 `folder.badge.plus` 改用 `SailuneSymbol.addVolume`。
- 保留 Label、繁體中文、tooltip、menu／button 形式、action、style 與位置。
- 驗證三處均使用共用 key、原圖標字串只在語意表中定義；通過 `git diff --check` 與主機無簽章 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同操作的圖標與呼叫統一；三處皆為新增卷。 |
| U | not_applicable | 不改圖形、文案、控制形式或位置。 |
| I | approved | 屬已授權 V9 共用語意資源採用，只更改圖標來源。 |

## 已完成

- 新增 `SailuneSymbol.addVolume`，對應原 `folder.badge.plus`。
- `BookOverviewView.swift` 兩處與 `EditorWorkspaceView.swift` 一處完成接入。
- 未改資料或操作結果；未執行 XCTest 或 GUI 驗收。
- `git diff --check` 與主機無簽章 macOS Debug build 通過。
