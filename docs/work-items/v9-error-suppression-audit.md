# V9 `try?` 失敗路徑盤點

> 狀態：closed（55 處靜態命中已按用途與風險分組）
>
> 工作單位：確認 V9 錯誤處理規則對目前程式的具體剩餘範圍。

## 目標與完成條件

- 依目前 `Sailune/**/*.swift` 的 `try?` 命中，區分可能改變資料正確性、可能遮蔽資訊，以及目前有意 fallback 的用途。
- 對每組指出來源碼失敗折疊、後續修正邊界及不能推論為缺陷的限制，存於 `docs/v9-error-suppression-audit.md`。
- 本批只修改文件，不改功能程式、不執行程式測試或 GUI 驗收；檢查索引與 `git diff --check`。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求 V9 內部程式規範化；本單是來源碼調查與文件整理。 |
| U | not_applicable | 不改畫面。 |
| I | not_applicable | 不改功能程式。 |

## 結果

- 2026-09-24：55 處靜態命中已分組；備份還原 rollback、能力建立補償、地圖／設定查詢與清理被列為優先；可選解析和取消睡眠保留具體理由。此數量是搜尋結果，不代表 55 個獨立缺陷。
- 2026-09-24：清單已連至 V9 採用矩陣與文件索引，`git diff --check` 通過；未執行程式測試或 GUI 驗收。
