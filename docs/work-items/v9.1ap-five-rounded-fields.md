# V9.1ap 五個固定圓角單行欄

> 狀態：active（五個 ID 已接線且 Debug build 通過，CJK／focus／首行 GUI 待驗收）；固定 ID：V9-0319、V9-0377、V9-1933、V9-2109、V9-2162。

- R：approved。使用者要求表單欄位共用化及首行安全檢查；五處均為一般文字與顯式 `.roundedBorder`。
- U：not_applicable。本批保留原文字、Binding、字體、更新／截字事件及原圓角樣式，沒有畫面提案變更。
- I：approved。依使用者要求按凍結清單處理剩餘項目，僅讓五個固定 ID 接入既有 `SailuneFormTextField`；不碰其他原生 Form、搜尋、alert、組字或首行多行編輯器。
- 完成條件：五個 ID 的原讀寫、modifier 次序與顯示文字不變；來源核對、無簽章 Debug build、`git diff --check`，CJK／首行／focus 的 GUI 驗收另記。

五個來源已核對接入共用欄位，原圓角由共用元件提供，外層字級和更新／截字事件保留；無簽章 macOS Debug build 與 `git diff --check` 通過。
