# V9.1j 編輯圖標語意接入

> 狀態：closed（七個既有編輯圖標已接入語意表）
>
> 工作單位：讓既有重新命名與編輯操作的鉛筆圖標引用同一語意資源。

## 目標與完成條件

- 核對既有七個 `pencil` 呼叫點的 action，涵蓋卷／節／時間軸重新命名，以及紀元／事件編輯；統一引用 `SailuneSymbol.edit`。
- 原 SF Symbol、按鈕文字、role、位置、命中、編輯及保存行為皆不變。
- 核對 raw `pencil` 只保留在語意表，通過差異檢查及 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已要求相同功能圖標統一；七個呼叫點均啟動既有編輯或重新命名流程。 |
| U | not_applicable | 字形、文字、位置和互動均保持原狀。 |
| I | approved | 使用者已指示完成 V9；本批僅將七個既有字面值替換為已存在的語意 key。 |

## 驗證與下一步

- 2026-09-23：七個呼叫點已引用 `.edit`；raw `"pencil"` 只留在 `SailuneSymbol`。原圖形、文字與 action 不變。
- 2026-09-23：`git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
