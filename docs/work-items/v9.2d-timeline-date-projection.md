# V9.2d 時間軸日期投影責任歸位

> 狀態：closed（日期投影與資料型別已移至專責檔）
>
> 工作單位：把已獨立命名的日期投影及其資料型別移出大型時間軸 View 檔。

## 目標與完成條件

- `TimelineCell`、`TimelineEraGroup`、`TimelineSlot`、`CellKind`、`CellAccum` 與 `TimelineDateProjection` 移至 `TimelineDateProjection.swift`；原宣告內容、函式簽名、存取層級、分組／排序／標籤演算法不變。
- `TimelinePanelView` 與現有 `ItemV3Tests`／`V42OutlineTests` 的型別名稱、API 和呼叫點不改；`TimelineOutlineSourceProjection` 另有 Store 讀取責任，暫留原檔，不混入日期投影。
- 不改時間軸 UI、資料保存、SwiftData schema 或事件選取行為。
- 逐段比對搬移內容、確認來源檔無重複型別、通過 `git diff --check` 與主機 Debug build。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求將內部程式責任規範化，讓新增與維護較容易；本批只整理已分離的日期投影責任。 |
| U | not_applicable | 不改畫面或互動。 |
| I | approved | 使用者已指示完成 V9；型別與函式逐字搬移，原 API 和呼叫端維持。 |

## 驗證與風險

- 2026-09-23：三段型別／投影宣告共 350 行逐字移至 `TimelineDateProjection.swift`；新檔除 `import Foundation` 外，與 `HEAD:TimelineViews.swift` 對應區塊完全一致。`TimelineOutlineSourceProjection` 仍留原檔。
- 2026-09-23：來源檔無重複宣告，`git diff --check` 與主機無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
- `TimelineDateProjection` 的日曆分組與標籤內容目前已由既有測試檔呼叫，但未因本批新增或執行測試。
