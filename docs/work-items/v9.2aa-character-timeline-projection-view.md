# V9.2aa 角色時間投影畫面責任歸位

> 狀態：closed
>
> 工作單位：將角色卡片時間投影及其事件列從大型 `TimelineViews.swift` 移入專用檔，維持原呈現與保存流程。

## 現況、範圍與非目標

- `CharacterTimelineProjectionView` 與私有 `EventProjectionRow` 位於 `TimelineViews.swift` 尾段，分別處理角色事件列表與主時間軸顯示開關。
- 本批只移動這兩個 View 及標記，保留 module 可見的角色投影入口和私有事件列。`Color(hex:)` 仍由時間軸共用延伸提供。
- 不改排序、顯示條件、字形文案、顏色、尺寸、hover、SwiftData 查詢、保存／rollback 或錯誤 alert；不改 schema。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權 V9 內部責任整理；本項只移動既有宣告。 |
| U | not_applicable | 不改 UI 呈現或互動。 |
| I | approved | 保留宣告正文與呼叫入口，以獨立檔案管理角色時間投影。 |

## 完成條件與驗證

- 新檔有且只有這兩個 View 宣告，原檔無重複；原呼叫點仍可編譯。
- 除必要 import、跨檔顯示名稱輔助函式的存取層級與檔案位置外，宣告正文與搬移前的目前工作樹內容一致。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果

- 208 行角色時間投影與事件列宣告已移至 `CharacterTimelineProjectionView.swift`；搬移段落 SHA-256 為 `079170bdb58c2a5f13ba7d069129286e0556b1b8e3ede5b61db33d99419f6c48`，搬移前後相同。
- 原檔只保留一份 `sailuneDisplayName` 輔助函式；因兩個檔案共同使用，其存取層級由 `private` 改為 module internal，函式正文不變。
- `rg` 確認兩個 View 各只宣告一次；`git diff --check` 與主機無簽章 macOS Debug build 通過。未執行 XCTest 或角色時間投影 GUI 驗收。
