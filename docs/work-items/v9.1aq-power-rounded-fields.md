# V9.1aq 勢力八個區塊圓角欄位

> 狀態：active（八個 ID 已接線且 Debug build 通過，GUI／CJK／首行待驗收）；固定 ID：V9-1547、1548、1549、1553、1557、1569、1588、1592。

- R：approved。使用者要求欄位共用化；八處均在父區塊套用原生 `.roundedBorder`，為一般單行文字欄。
- U：not_applicable。文字、Binding、表面、圓角與同列順序不變；不處理勢力成員內的原生職務欄。
- I：approved。八處改呼叫 `SailuneFormTextField`。識別與優勢區塊移除只為這些欄位設定的重複父層樣式；生命週期區保留父層 style，以免影響 nested `CharacterNodePicker`。
- 完成條件：八個固定 ID 的 Binding 保存與原版一致，source、Debug build、`git diff --check` 通過；GUI／CJK／首行仍另驗收。

八處接線與來源已核對；識別及優勢區塊移除只作用於本組欄位的父層圓角樣式，生命週期區塊保留父層 style。無簽章 macOS Debug build 與 `git diff --check` 通過。
