# V9.1al AI 閱讀範圍與角色整理圖標共用

> 狀態：closed
>
> 工作單位：同一 AI 功能的選單入口與已選內容摘要引用同一語意圖標。

## 範圍與非目標

- AI 側欄「加入閱讀範圍…」與已選閱讀範圍摘要均使用 `doc.text.magnifyingglass`；「角色資訊整理…」與已選模板摘要均使用 `person.text.rectangle`。
- 各加一個 `SailuneSymbol` 語意鍵，四個直接呼叫點改用 key。字形、文案、位置、選取／送出動作與資料不變。
- 角色基本資訊區也使用 `person.text.rectangle`，但用途不同，保留原呼叫；其餘資料類型字形不批量合併。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能共用圖標與程式碼。 |
| U | not_applicable | 四處字形與畫面均不變。 |
| I | approved | 只新增兩個語意鍵並替換 AI 側欄四個字串參數。 |

## 完成條件

- 同一 AI 功能的入口與摘要引用相同語意鍵；角色基本資訊仍獨立。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；不新增或執行 XCTest。

## 結果與驗證

- AI 側欄四處已引用 `.aiReadingScope`／`.aiCharacterTemplate`；兩種原 SF Symbol 字形不變。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；`SailuneSymbol` 靜態呼叫點 178，直接 `systemName`／`systemImage` 字串 104。
- 未執行 XCTest 或 GUI 驗收；角色基本資訊的相同字形保留獨立用途。
