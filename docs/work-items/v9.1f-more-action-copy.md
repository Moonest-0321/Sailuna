# V9.1f 刪除／新增／關閉文字接入

> 狀態：closed（47 個通用文字呼叫點已接入）
>
> 工作單位：將另外三種語意明確的通用操作文字接到同一資源。

## 目標與完成條件

- 31 個 `Button("刪除"...)`、4 個 `Button("新增"...)`、7 個 `Button("關閉"...)` 與 4 個 `Label("刪除"...)`、1 個 `Label("新增"...)` 使用對應的 `SailuneActionCopy`。
- 既有兩個帶 `xmark` 的「關閉」按鈕、帶 `plus` 的「新增」Label 使用相同語意的 `SailuneSymbol`；圖形不變。
- 保留按鈕文字值、role、action、快捷鍵、tooltip、位置和確認流程。工作區模式 `Label("編輯"...)` 屬導覽名稱，不當作通用「編輯」操作。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能使用相同敘述文字與圖標，並統一引用共用程式碼。 |
| U | not_applicable | 本批僅以相同中文與 SF Symbol 值替換資源來源。 |
| I | approved | 使用者已指示完成 V9；範圍限定三種通用動詞與既有圖形。 |

## 驗證與下一步

- 2026-09-23：31 個刪除、4 個新增、7 個關閉 Button，以及 4 個刪除、1 個新增 Label 已引用共用文字資源；兩個關閉圖標與一個新增圖標引用同語意 `SailuneSymbol`，原呼叫的文字與字形不變。
- 2026-09-23：建置產物確認 `action.delete`／`action.add`／`action.close` 仍分別為「刪除」／「新增」／「關閉」。`git diff --check` 與主機環境無簽章 Debug build 通過；未執行程式測試或 GUI 驗收。
