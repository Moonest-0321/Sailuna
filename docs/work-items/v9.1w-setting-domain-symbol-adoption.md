# V9.1w 設定領域圖標語意統一

> 狀態：closed（設定領域的 10 個圖標呼叫點已接入具名 key）
>
> 工作單位：集中設定區能力、物品、資源與勢力層級的重複圖標來源。

## 目標與範圍

- 為能力、物品、資源、勢力層級新增 `SailuneSymbol` 的領域語意 key。
- 將 `SidebarSettingKey`、Inspector、設定層級管理／選取、資源空狀態／選擇入口的同領域圖標改用 key。
- 保留原 `sparkles`、`shippingbox`、`list.number` 字形，保留標題、內容、布局及操作；AI 助手的 `sparkles` 保持獨立呼叫。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同功能圖標集中定義；本批限定四種明確設定領域圖標。 |
| U | not_applicable | SF Symbol 與所有文案、版位、流程不變，只更換圖標來源。 |
| I | approved | 屬 V9 共用語意資源採用；不改設定資料或操作行為。 |

## 驗收與驗證

- `ability`、`item`、`resource`、`powerLevel` 各自集中提供既有 SF Symbol。
- 設定領域的十個既有呼叫點引用對應 key；AI 助手的同圖形保留獨立語意。
- `git diff --check` 與無簽章 macOS Debug build 通過；無程式測試或 GUI 行為改動。
