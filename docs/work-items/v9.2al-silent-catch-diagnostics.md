# V9.2al 三個固定靜默 catch 的診斷

> 狀態：active（程式接線與 Debug build 已完成，隔離故障注入待驗收）；固定 ID：V9-0686、V9-0737、V9-1058。

- R：approved。使用者要求依凍結清單處置剩餘程式碼規範；此前核定錯誤只記錄、不改操作結果。
- U：not_applicable。沒有畫面與互動變更。
- I：approved。只在三個現有 catch 加入 private OSLog；原刪除未保存角色後回傳 `nil`、Inspector 導覽退回列表、規劃投影一致性修復待下次重試的結果全部維持。
- 驗收：三個 ID 均有 private 診斷、主機 Debug build 與 `git diff --check` 通過；故障注入仍須在隔離資料驗證，不能因有 log 宣稱已恢復資料。

三個 catch 已加 private OSLog，現行來源與原結果分支逐項核對，無簽章 macOS Debug build 通過。尚未執行隔離故障注入。
