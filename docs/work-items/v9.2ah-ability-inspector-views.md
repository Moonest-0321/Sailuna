# V9.2ah 能力 Inspector 畫面責任歸位

> 狀態：closed
>
> 工作單位：將能力清單與詳情 View、能力建立補償診斷 logger 從 Inspector 導覽檔移至能力專責檔。

## 現況與範圍

- `InspectorViews.swift` 中的 `AbilityDetailView` 與 `AbilityListContainerView` 連續成一個完整能力畫面區；根導覽跨區呼叫兩個 View。
- 能力建立補償失敗的 `abilityCreationLogger` 只由能力清單使用，應與該流程歸位。
- 本批只搬移宣告與 logger，兩個跨檔 View 入口由 file-private 改為 module internal；原查詢、建立、跨 Store 補償、刪除、錯誤提示及 UI 不變。
- 不改 schema、能力連結、角色正文引用、歷史定位或已核定的「只記錄錯誤、不改操作結果」政策。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者已授權 V9 內部責任整理；能力區有明確獨立於 Inspector 導覽的畫面與診斷責任。 |
| U | not_applicable | 僅搬移宣告，不改畫面或互動。 |
| I | approved | 使用目前工作樹的完整宣告，僅改兩個跨檔入口可見度與新檔 import。 |

## 完成條件

- 能力兩個 View 與補償 logger 各保留一份，根導覽原呼叫可編譯。
- 搬移正文除兩個 `private` 外與搬移前目前工作樹一致，資料結果政策不變。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。

## 結果與驗證

- 能力清單與詳情 236 行宣告已移至 `AbilityInspectorViews.swift`；還原兩個跨檔入口的 `private` 後，搬移段落 SHA-256 與搬移前工作樹同為 `cbefc1d9a6337d5edee08e371656a6d5ef0329553af64dd743fef6a7a511737d`。補償診斷 logger 亦原樣移入新檔。
- 兩個 View 和 logger 各僅宣告一次；`InspectorRootView` 原呼叫點不變。
- `git diff --check` 與主機無簽章 macOS Debug build 通過；未執行 XCTest、故障注入或 GUI 驗收。
