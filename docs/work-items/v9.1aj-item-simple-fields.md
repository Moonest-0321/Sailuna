# V9.1aj 物品單行欄位共用接線

> 狀態：closed
>
> 工作單位：將物品相關畫面六個相同的原生圓角單行欄位接入共用元件。

## 現況、範圍與非目標

- 新增物品名稱、物品詳情名稱與分類、副本歷史事件、物品等級名稱與選填物品名稱，均直接使用 `TextField` 加 `.roundedBorder`。
- 六欄接入 `SailuneFormTextField`，保留各自的 placeholder、Binding、排列、外層驗證與保存／刪除操作。
- 不調整視覺尺寸、顏色、圓角、動作或資料結構；首行穿模稽核仍由 V9.1x 管理。

## R／U／I

| 關卡 | 狀態 | 依據 |
|---|---|---|
| R | approved | 使用者要求相同欄位共用程式碼。 |
| U | not_applicable | 共用元件仍提供原生 `TextField` 與同一個圓角 style。 |
| I | approved | 六處只替換欄位建構式，不變更其父層或模型規則。 |

## 完成條件

- 六處文字與 Binding 對應原欄位，驗證、保存、排序與資料操作不變。
- `git diff --check` 及主機無簽章 macOS Debug build 通過；不新增或執行 XCTest。
- 實際輸入、首行、Light／Dark 留待 V9.1x 實機驗收。

## 結果與驗證

- 六處已改用 `SailuneFormTextField`；原文字、Binding、外層驗證與資料操作保留。
- 主機無簽章 macOS Debug build 與 `git diff --check` 通過；共用單行欄位 32 處、精確原生 `TextField(` 72 處、`.roundedBorder` modifier 35 處（靜態命中）。
- 未執行 XCTest 或 GUI 輸入驗收。
